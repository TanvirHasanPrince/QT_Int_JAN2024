/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "www.api.technicaltest.quadtheoryltd.com",
      "images.pexels.com",
      "images.unsplash.com",
    ],
  },
};

module.exports = nextConfig
